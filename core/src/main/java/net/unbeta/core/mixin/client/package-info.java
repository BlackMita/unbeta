/**
 * Client-only mixins. Empty during M0-M2.
 *
 * <p>When the first client mixin lands, re-add this to fabric.mod.json:
 * <pre>{@code { "config": "unbeta.client.mixins.json", "environment": "client" } }</pre>
 */
package net.unbeta.core.mixin.client;
